#ifndef MY_MATH
#define MY_MATH

/**
 * \brief My sqrt function.
 * \param x Input parametr.
 * \param eps Pricision for calculations.
 * \return sqrt(x) with precison eps.
 */
double mysqrt(double x, double eps) noexcept;

/**
 * \brief My sin function.
 * \param x Input parametr.
 * \param eps Pricision for calculations.
 * \return sin(x) with precison eps.
 */
double mysin(double x, double eps) noexcept;

/**
 * \brief My cos function.
 * \param x Input parametr.
 * \param eps Pricision for calculations.
 * \return cos(x) with precison eps.
 */
double mycos(double x, double eps) noexcept;

/**
 * \brief My atan function.
 * \param x Input parametr.
 * \param eps Pricision for calculations.
 * \return atan(x) with precison eps.
 */
double myatan(double x, double eps) noexcept;

/**
 * \brief My exp function.
 * \param x Input parametr.
 * \param eps Pricision for calculations.
 * \return exp(x) with precison eps.
 */
double myexp(double x, double eps) noexcept;

#endif // MY_MATH
